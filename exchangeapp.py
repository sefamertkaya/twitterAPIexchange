import requests
import tweepy
import json
import datetime
import locale


locale.setlocale(locale.LC_ALL, 'Turkish')

an = datetime.datetime.now()

tarih = datetime.datetime.strftime(an, '%d/%m/%y  %H:%M:%S')

url = " ???????????????????????????"

response = requests.get(url)

json_verisi=response.json()

esas=json_verisi[?????]

esas=esas["??????"]

consumer_keys='???????????'

consumer_secrets='???????????'

access_tokens='??????????????'

access_secrets='????????????????????'

auth=tweepy.OAuthHandler(consumer_keys,consumer_secrets)

auth.set_access_token(access_tokens,access_secrets)

api=tweepy.API(auth)

api.update_status('1 Euro ='+ str(esas) + " TL \n \n Tarih/Saat : " + str(tarih) )
